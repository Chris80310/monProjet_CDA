{% extends 'base.html.twig' %}

{% block title %}Hello ProfilController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{# <div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/home/christopher/Bureau/GitHub/monProjet_CDA/src/Controller/ProfilController.php'|file_link(0) }}">src/Controller/ProfilController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/home/christopher/Bureau/GitHub/monProjet_CDA/templates/profil/index.html.twig'|file_link(0) }}">templates/profil/index.html.twig</a></code></li>
    </ul>
</div> #}

 <div class="container">
        <div class="row mt-5">
            <div class="col-sm-8 offset-sm-2">
                <h1>Vos informations</h1>
                <table class="table table-light table-hover">
                    <thead>
                    <tr>
                        <th>Prenom</th>
                        <th>Nom</th>
                        <th>Email</th>
                        <th colspan="2">Rôle(s)</th>
                        <th>Adresse</th>
                    </tr>
                    </thead>
                    <tbody>
                    <tr>
                        <td>{{ informations.prenom }}
                        <td>{{ informations.nom }}
                        <td>{{ informations.email }}
                        {# <br><br><button href="" class="#">Modifier l'email</button> #}
                        </td>

                        {% for adresse in informations.adresse %}
                            <td>{{ adresse }}</td>
                        {% endfor %}
                        
                        {# {% for adresse in informations.adresse %}
                          <td>{{ adresse }}</td>
                        {% endfor %} #}

                        {% for role in informations.roles %}
                          <td>{{ role }}</td>
                        {% endfor %}
                    </tr>
                    </tbody>
                </table>

            </div>
        </div>
    </div>
    
{% endblock %}
