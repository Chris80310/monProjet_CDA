{% extends 'base.html.twig' %}

{% block body %}

	<div class="container-fluid">

		<h1 class="mt-3">Accueil</h1>

		<div
			class="d-flex justify-content-around my-5">
			{# <div class="row"> #}
                {% for cat in categorie %}
                    <div class="col-12 col-sm-6 col-md-4 col-xl-3 mb-3 ">
                        <a href="{{path('app_scat', {"id" : cat.id })}}">
                            <div class="card d-flex align-items:center">
                                <img src="{{asset ('assets/img/cat/' ~ cat.getImgCat())}}" alt="image" srcset="" class="card-img-top">
                                <div class="card-body">
                                    <h5 class="card-titl text-center">{{ cat.getLibelleCat() }}</h5>
                                </div>
                            </div>
                        </a>
                    </div>
                    {# {{ dump(cat) }} #}
                {% endfor %}
			{# </div> #}
		</div>
	</div>
{% endblock %}
